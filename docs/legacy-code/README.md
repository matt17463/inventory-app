# Archived legacy source

These files were removed from deployable application locations during Steps 9–11. JavaScript/JSX copies are stored as `.txt` only for rollback/reference and are not bundled by Vite or deployed as Netlify functions.

The old root `supabase_feature_updates_incremental_samples_bins.sql` is also archived as `.txt` because it represents the superseded sample/inventory model and should not be rerun against the current production schema.

The active replacement paths are documented in `docs/STEPS_6_14_SAFE_DEPLOYMENT.md`.
