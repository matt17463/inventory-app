# Generated Supabase Types

Run `bash scripts/step2_generate_types.sh` after setting `SUPABASE_PROJECT_REF`.

The generated `database.generated.ts` file reflects the actual production schema and should be committed after review. It is not generated inside this package because the production project is not accessible from the build environment.
