# Deployment folder

Use `DEPLOYMENT_GUIDE_NOVICE.md` at the project root.

The `sql` folder contains numbered copies of the SQL files used for the current deployment. Start with `00_FINAL_READ_ONLY_AUDIT.sql`.
